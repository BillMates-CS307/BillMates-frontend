import styled from '@emotion/styled'

export const AppBar = () => <AppBarWrapper>AppBar</AppBarWrapper>

const AppBarWrapper = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 0 24px;
`
